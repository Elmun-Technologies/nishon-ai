version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: nishon_postgres
    environment:
      POSTGRES_DB: nishon_ai_db
      POSTGRES_USER: nishon
      POSTGRES_PASSWORD: nishon_secret
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U nishon -d nishon_ai_db']
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - nishon_network

  redis:
    image: redis:7-alpine
    container_name: nishon_redis
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - nishon_network

  api:
    build:
      context: ../apps/api
      dockerfile: Dockerfile
    container_name: nishon_api
    ports:
      - '3001:3001'
    env_file:
      - ../.env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - nishon_network
    volumes:
      - ../apps/api:/app
      - /app/node_modules

  web:
    build:
      context: ../apps/web
      dockerfile: Dockerfile
    container_name: nishon_web
    ports:
      - '3000:3000'
    environment:
      - NEXT_PUBLIC_API_URL=http://api:3001
    depends_on:
      - api
    networks:
      - nishon_network
    volumes:
      - ../apps/web:/app
      - /app/node_modules
      - /app/.next

volumes:
  postgres_data:
  redis_data:

networks:
  nishon_network:
    driver: bridge